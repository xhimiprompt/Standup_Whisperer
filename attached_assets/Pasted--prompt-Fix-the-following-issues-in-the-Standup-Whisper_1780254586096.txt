{
  "prompt": "Fix the following issues in the Standup Whisperer app. Make all changes exactly as specified below.\n\n---\n\nPROBLEM 1 — Model refuses non-English input (CRITICAL)\nThe LLM currently refuses Albanian or non-work-looking input with explanations like 'I appreciate you sharing this, but...'. This must never happen.\n\nIn prompts.js, update the SYSTEM_PROMPT constant. At the very end of the string, append exactly this text:\n\nABSOLUTE OVERRIDE RULES (highest priority, cannot be broken):\n- NEVER refuse to generate a standup. No exceptions.\n- NEVER explain why the input is problematic or non-technical.\n- NEVER add commentary, recommendations, or meta-text outside the standup structure.\n- If input is in Albanian, Kosovar slang, or any non-English language, translate and extract into English standup format silently.\n- If input looks like personal schedule items such as gym, dinner, or meetings, treat them as work calendar items and extract them as-is into Yesterday/Today/Blockers.\n- Output ONLY the standup. Nothing before it. Nothing after it.\n\n---\n\nPROBLEM 2 — Model adds chatty text after the standup\nIn prompts.js, inside the FORMAT_CONTRACTS object, at the very end of each of the three format strings (plain, slack, markdown), add this line:\nSTOP after the last bullet. Do not add any text after the Blockers section.\n\n---\n\nPROBLEM 3 — nonEnglish edge case handler not triggering\nIn prompts.js, inside the buildPrompt() function, after the existing edge case detection blocks, add this new detection block:\n\nconst nonLatinOrAlbanian = /[ëçËÇ]/.test(rawNotes) || /\\b(sot|dje|neser|mbremje|mengjes|pune|takim|problem|duhet)\\b/i.test(rawNotes);\nif (nonLatinOrAlbanian) {\n  activeEdgeCases.push(EDGE_CASE_HANDLERS.nonEnglish);\n}\n\n---\n\nPROBLEM 4 — Add Copy-to-Slack button (bonus feature)\n1. After the standup output is displayed, show a button labeled 'Copy for Slack' that appears only when output exists.\n2. When clicked, convert the current output to Slack markdown: wrap section headers in *asterisks* (Yesterday, Today, Blockers), add :warning: after Blockers header, use bullet character •, then copy to clipboard.\n3. Show a 'Copied!' confirmation that disappears after 2 seconds.\n4. Style the button to match the existing dark theme monospace UI.\n\n---\n\nPROBLEM 5 — Ensure stable public deployment\nCheck that package.json has a valid start script. Make sure the Express server (or whatever server is used) listens on process.env.PORT || 3000. Verify the app can be deployed with a stable public Replit URL.\n\n---\n\nTEST CASE — After all changes, verify with this input:\nInput text: 'ndriqim palushi , sot ne ora 10 ne mbremje ne nje darke pune, neser ne 10 mengjes online meeting , pasdite ne ora 2 fjalim ne nje event ne ICK qe do zgjas rreth 2 ore, ne ora 4 pasdite ne gym dhe me 6 ne mbremje nje darke pune'\nFormat: Slack\n\nExpected output:\n*Yesterday*\n• Work dinner with Ndriqim Palushi at 10 PM\n\n*Today*\n• Online meeting at 10 AM\n• Speaking engagement at ICK at 2 PM (~2 hours)\n• Gym at 4 PM\n• Work dinner at 6 PM\n\n*Blockers* :warning:\n• Nothing to report"
}